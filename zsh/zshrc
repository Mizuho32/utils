# color
autoload colors 
colors

# prompt
local info="%B%F{green}%n%f%F{red}@%f%F{cyan}%m%f%b" 
local prom="%B%(?,%F{green},%F{red})%(!, #>, $>)%f%b" 
local dir=$'\n'"%B%F{yellow}[%~]%f%b"$'\n' 
PROMPT="${dir}${info}${prom}"


# ls
export LSCOLORS=gxfxcxdxbxegedabagacag
export LS_COLORS='di=36;40:ln=35;40:so=32;40:pi=33;40:ex=31;40:bd=34;46:cd=34;43:su=30;41:sg=30;46:tw=30;42:ow=30;46'

# 補完候補もLS_COLORSに合わせて色が付くようにする
zstyle ':completion:*:default' list-colors ${(s.:.)LS_COLORS}

# lsがカラー表示になるようエイリアスを設定
case "${OSTYPE}" in
			linux*)
			  # Linux
			  alias ls='ls -F --color'
			  ;;
esac


# 補完
autoload -U compinit
compinit

<<<<<<< HEAD
#################### ALIAS ################
alias sudo="sudo -E"
alias open="xdg-open"
alias here="./"
alias gcc="gcc -Wall"
alias sl="sl -e"

########################### EXPORTS ##################
export PATH=$PATH:~/bin:~/.local/bin
export PATH="$HOME/.rbenv/bin:$PATH:$HOME/usr/local/redpen/bin"
export RUBYLIB="$RUBYLIB:$HOME/lib/ruby"
export JAVA_HOME=/usr/lib/jvm/java-8-oracle
export RBENV_ROOT="$HOME/.rbenv"
export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
export GIT_EDITOR=vim

# rbenv pyenv
eval "$(rbenv init -)"
eval "$(pyenv init -)"

#nvm
#. ~/.nvm/nvm.sh
alias get_nvm=". ~/.nvm/nvm.sh"


################ Command not Found ####################
source /etc/zsh_command_not_found

# xv6
export TOOLPREFIX="/media/Windows/Users/Mizuho_2/Ubuntu/usr/local/bin/i386-jos-elf-"

# mruby
export PATH=$PATH:$HOME/usr/local/mruby/bin

# esp32
export PATH=$PATH:$HOME/usr/local/xtensa-esp32-elf/bin
export IDF_PATH=$HOME/usr/local/esp-idf


export GIT_EDITOR=vim

#################### ssh Login prompt #####################
# Set DISPLAY ENV?
if [ "${SSH_CLIENT}" != "" ]; then
  echo ""
	echo -n 'Set $DISPLAY To localhost:N.0 ? [0,1... / n] >>'
	read input

	if [ "${input}" != "n" -a "${input}" != "" ]; then
			export DISPLAY="localhost:${input}.0"
	fi
fi

# Tmux select
ZSHRC="$HOME/.zshrc"
if [ -L $ZSHRC ] ; then
    ZSHRC_DIR=$(dirname $(readlink -f $ZSHRC)) ;
else
    ZSHRC_DIR=$(dirname $ZSHRC) ;
fi ; 

$ZSHRC_DIR/tmux.rb
